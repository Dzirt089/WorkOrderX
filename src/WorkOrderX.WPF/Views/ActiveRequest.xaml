<Page x:Class="WorkOrderX.WPF.Views.ActiveRequest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WorkOrderX.WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="700"
      Title="ActiveRequest">

	<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
		<Border BorderThickness="2" CornerRadius="8" BorderBrush="DodgerBlue" Margin="10">
			<ListView ItemsSource="{Binding ActiveRequests, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
					  SelectedItem="{Binding SelectedRequest, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					   FontSize="14" BorderThickness="0">
				<ListView.ItemContainerStyle>
					<Style TargetType="ListViewItem">
						<Setter Property="Margin" Value="0,0,0,5" />
						<Setter Property="Padding" Value="5" />
						<Setter Property="BorderThickness" Value="1" />
						<Setter Property="BorderBrush" Value="LightBlue" />
						<Setter Property="Background" Value="Transparent" />
						<Setter Property="HorizontalContentAlignment" Value="Stretch" />

						<Style.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="Background" Value="DodgerBlue" />
								<Setter Property="Foreground" Value="Black" />
							</Trigger>
							<Trigger Property="IsSelected" Value="True">
								<Setter Property="Background" Value="LightGreen"/>
							</Trigger>
						</Style.Triggers>
					</Style>
				</ListView.ItemContainerStyle>
				<ListView.Resources>
					<Style TargetType="GridViewColumnHeader">
						<Setter Property="HorizontalContentAlignment" Value="Center" />
						<Setter Property="FontWeight" Value="Bold" />
						<Setter Property="Padding" Value="5" />

						<Setter Property="BorderThickness" Value="1" />
						<Setter Property="BorderBrush" Value="DodgerBlue" />
					</Style>
					<Style TargetType="TextBlock">
						<Setter Property="VerticalAlignment" Value="Center" />
						<Setter Property="TextWrapping" Value="Wrap" />
						<Setter Property="Margin" Value="5,0" />
					</Style>
				</ListView.Resources>
				<ListView.View>
					<GridView>
						<GridViewColumn Header="Заявка №" DisplayMemberBinding="{Binding Path=ApplicationNumber}"  Width="70"/>
						<GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Path=ApplicationStatus}" Width="150"/>
						<GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Path=CreatedAt}" Width="160"/>
						<GridViewColumn Header="План" DisplayMemberBinding="{Binding Path=PlannedAt}" Width="160"/>
						<GridViewColumn Header="Заказчик" DisplayMemberBinding="{Binding Path=CustomerEmployee.Name}" Width="150"/>
						<GridViewColumn Header="Важность" DisplayMemberBinding="{Binding Path=Importance}" Width="90"/>
					</GridView>
				</ListView.View>
			</ListView>
		</Border>
	</ScrollViewer>
</Page>
